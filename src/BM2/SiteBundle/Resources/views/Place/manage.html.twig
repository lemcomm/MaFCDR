{% extends "BM2SiteBundle::layout.html.twig" %}

{% block content %}
	<h2>{{ 'place.manage.name'|trans({},"places")|title }}</h2>
	<p>{{ 'place.manage.intro'|trans({},"places")|raw }}</p>
	<form class="wide" action="{{ path('bm2_site_place_manage') }}" method="post"
		{{ form_widget(form) }}
		<button name="submit">{{ 'submit'|trans({},"places") }}</button>
	</form>
{% endblock %}
