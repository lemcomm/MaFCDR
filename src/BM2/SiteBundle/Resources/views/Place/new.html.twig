{% extends "BM2SiteBundle::layout.html.twig" %}

{% block content %}
	<h2>{{ 'new.name'|trans({},"places")|title }}</h2>
	<p>{{ 'new.intro'|trans({},"places")|raw }}</p>
	<form class="wide" action="{{ path('bm2_site_place_new') }}" method="post"
		{{ form_widget(form) }}
		<button name="submit">{{ 'submit'|trans({},"places") }}</button>
	</form>
{% endblock %}
