{% extends "BM2SiteBundle::layout.html.twig" %}

{% block content %}
	<h2>{{ "control.changeoccupier.name"|trans({},"actions")|title }}</h2>

		<p>{{ "control.changeoccupier.intro"|trans({"%settlement%":link(settlement)},"actions")|raw}}</p>
		<p>{{ "control.changeoccupier.current"|trans({"%settlement%":link(settlement), "%realm%":link(settlement.occupier)},"actions")|raw}}</p>

		{{ form(form) }}

{% endblock %}
