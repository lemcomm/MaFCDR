{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ "control.rename.name"|trans({},"actions")|title }}</h2>
	{% if result is defined %}
		<p>
		{% if result.success %}
			{% if result.immediate %}
				{{ "control.rename.success"|trans({"%newname%":newname},"actions")|raw }}
			{% else %}
				{{ "control.rename.queued"|trans({"%newname%":newname},"actions")|raw }}
			{% endif %}
		{% else %}
			{{ result.message|trans({},"actions") }}
		{% endif %}
		</p>
	{% else %}
		<p>{{ "control.rename.intro"|trans({"%type%":settlement.getType|trans},"actions")|raw }}</p>
		{{ form(form) }}
	{% endif %}

{% endblock %}
