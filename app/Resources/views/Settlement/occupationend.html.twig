{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ "control.occupationend.name"|trans({},"actions")|title }}</h2>

	<p>{{ "control.occupationend.intro"|trans({"%settlement%":link(settlement)},"actions")|raw}}</p>

	{{ form(form) }}

{% endblock %}
