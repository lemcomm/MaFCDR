{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ "control.changeoccupant.name"|trans({},"actions")|title }}</h2>

	<p>{{ "control.changeoccupant.intro"|trans({"%settlement%":link(settlement)},"actions")|raw}}</p>

	{{ form(form) }}

{% endblock %}
