{% extends "layout.html.twig" %}

{% block content %}
<h2>{{ 'unit.recall.name'|trans({"%name%":unit.settings.name},"actions")|title }}</h2>
<p>{{ 'unit.recall.intro'|trans({"%name%":unit.settings.name},"actions")|raw }}</p>
	{{ form(form) }}

{% endblock %}
