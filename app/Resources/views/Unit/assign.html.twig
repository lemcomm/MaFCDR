{% extends "layout.html.twig" %}

{% block content %}
<h2>{{ 'unit.assign.name'|trans({"%name%":unit.settings.name},"actions")|title }}</h2>
<p>{{ 'unit.assign.intro'|trans({"%name%":unit.settings.name},"actions")|raw }}</p>
	{{ form(form) }}

{% endblock %}
