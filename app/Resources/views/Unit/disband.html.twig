{% extends "layout.html.twig" %}

{% block content %}
<h2>{{ 'unit.disband.name'|trans({"%name%":unit.settings.name},"actions")|title }}</h2>
<p>{{ 'unit.disband.intro'|trans({"%name%":unit.settings.name},"actions")|raw }}</p>
	{{ form(form) }}

{% endblock %}
