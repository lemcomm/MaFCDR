{% extends "BM2SiteBundle::layout.html.twig" %}

{% block content %}
<h2>{{ 'unit.return.name'|trans({"%name%":unit.settings.name},"military")|title }}</h2>
<p>{{ 'unit.return.intro'|trans({"%name%":unit.settings.name},"military")|raw }}</p>
	{{ form(form) }}

{% endblock %}
