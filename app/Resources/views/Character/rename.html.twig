{% extends "layout.html.twig" %}

{% block content %}
	<h2>{{ "meta.rename.name"|trans({},"actions")|title }}</h2>
	{% if result is defined %}
		<p>
		{% if result.success %}
			{{ "meta.rename.success"|trans({"%newname%":newname},"actions")|raw }}
		{% else %}
			{{ result.message|trans({},"actions") }}
		{% endif %}
		</p>
	{% else %}
	<p>{{ "meta.rename.intro"|trans({},"actions")|raw }}</p>
	<form class="wide" action="{{ path('bm2_site_character_rename') }}" method="post" {{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" name="submit">{{ "meta.rename.submit"|trans({},"actions") }}</button>
	</form>
	{% endif %}



{% endblock %}
